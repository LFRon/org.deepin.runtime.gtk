version: "1"

package:
  id: org.deepin.runtime.gtk4
  name: Linyaps GTK4 Runtime
  kind: runtime
  version: 25.2.0.1
  description: |
    GTK4 is a cross-platform C application framework. GTK's primary feature
    is its rich set of widgets that provide standard and beautiful GUI functionality.

base: org.deepin.base/25.2.0

build: |
  bash ./build.bash

sources:
  # linglong:gen_deb_source sources arm64 https://community-packages.deepin.com/deepin/beige crimson main commercial community
  # linglong:gen_deb_source install libgtk-4-1, libgtk-4-common, libwebkit2gtk-4.1-0, libgtk-4-media-gstreamer
